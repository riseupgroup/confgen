{{#if note}}# Note: {{note}}{{/if}}
protocol bgp {{name}}_v6 from dnpeers {
    neighbor {{peer_ll_ipv6}}%dn42peer{{num}} as {{asn}};
};

{{#unless mp_bgp}}
protocol bgp {{name}} from dnpeers {
    neighbor {{peer_ll_ipv4}} as {{asn}};
};
{{/unless}}